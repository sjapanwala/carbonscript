// reading from the console is almost like writing!
// we use the keyword "stdin" which means standard input
// this will save the input as a variable, with the type you set
// it will show user what you are expecting

stdin age;int How Old Are You?

// here we are calling the stdin function, setting age as a variable
// taking it as an intiger, and what ever is after is added to the prompt
// makesure to add the right type or else it will undefine

